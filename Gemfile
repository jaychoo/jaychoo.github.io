source "https://rubygems.org"

# This matches the Jekyll version and plugins GitHub Pages runs in production.
# See: https://pages.github.com/versions/
gem "github-pages", group: :jekyll_plugins

# Local preview server (needed on Ruby 3+; GitHub Pages itself doesn't need this).
gem "webrick"
